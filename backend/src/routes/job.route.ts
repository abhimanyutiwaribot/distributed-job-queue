import { Router } from "express";

export const jobRouter = Router();

// get all jobs
jobRouter.get("/jobs", () => {

})

// get a job
jobRouter.get("/jobs/:id", () => {

})

// create a job
jobRouter.post("/jobs", () => {
  
})